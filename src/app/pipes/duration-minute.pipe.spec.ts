import { DurationMinutePipe } from './duration-minute.pipe';

describe('DurationMinutePipe', () => {
  it('create an instance', () => {
    const pipe = new DurationMinutePipe();
    expect(pipe).toBeTruthy();
  });
});
